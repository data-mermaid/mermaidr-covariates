# List available covariates

List available covariates

## Usage

``` r
list_covariates(as_data_frame = TRUE)
```

## Arguments

- as_data_frame:

  Whether to return the covariates as a data frame. Defaults to `TRUE`.

## Examples

``` r
# list_covariates()
```
